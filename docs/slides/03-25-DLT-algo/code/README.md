# To compile and run

## Install dependencies

``` shellsession
sudo apt install $(cat apt.txt)
```

## Compile

``` shellsession
mkdir build
cd build
cmake ..
make
```

## Run

``` shellsession
./homography
```
